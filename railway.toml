[build]
command = '''
  npm install --production
'''

[deploy]
startCommand = "node api-server.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# MatchPro v11.0.0 | Crystal Power Investments

